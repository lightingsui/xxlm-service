<cu-custom bgColor="bg-gradual-pink" isBack="{{true}}">
  <view slot="backText">返回</view>
  <view slot="content">公告发表</view>
</cu-custom>

<view class="cu-card dynamic {{isCard?'no-card':''}}">
  <view class="cu-item shadow" style="height:1130rpx">

    <view class="cu-list menu-avatar">
      <view class="cu-item">
        <view class="cu-avatar round lg" style="background-image:url(https://ossweb-img.qq.com/images/lol/web201310/skin/big10006.jpg);"></view>
        <view class="content flex-sub">
          <view>{{name}}</view>
        </view>
      </view>
    </view>

    <view class="cu-form-group">
      <view class="title">标题</view>
      <input bindinput="titleInput" placeholder="请输入公告标题" value="{{title}}"></input>
    </view>

    <view class="cu-form-group">
		<view class="title">类型</view>
      <picker bindchange="PickerChange" value="{{index}}" range="{{picker}}">
        <view class="picker">
          {{index?picker[index]:'请选择'}}
        </view>
      </picker>
    </view>

    <view class="cu-form-group align-start" style="height:700rpx"> 
      <view class="title">内容</view>
      <textarea maxlength="-1" bindinput="textInput" placeholder="请输入公告内容" value="{{text}}"></textarea>
    </view>


    <view class="cu-form-group">
      <view class="flex solid-bottom justify-center">
          <button class="cu-btn bg-gradual-pink  margin-tb-sm round publish" bindtap="publish">
          发表
          </button>
        </view>
    </view>
    
  </view>
</view>

     <input class="weui-input" bindinput="inputedit" value="{{info}}" />
